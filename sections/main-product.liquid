

  <script src="{{ 'product-info.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>


<div class="main-product-template">
<h2> Hello this is main product </h2>
<p>test</p>
{% assign current_product = product.selected_or_first_available_variant %}
{% assign product_image = current_product.featured_image %}
<div class="container">
<div class="row">
<div class="col-md-6">

  {% assign current_product = product.selected_or_first_available_variant %}
{% assign product_image = current_product.featured_image %}

<!-- Check variant image -->
<p>Variant Image URL: {{ product_image | img_url: 'large' }}</p>

<!-- Check product image -->
<p>Product Image URL: {{ product.featured_image | img_url: 'large' }}</p>

{% if product_image %}
  <img class="product-image" src="{{ product_image | img_url: 'large' }}" alt="{{ product_image.alt | escape }}" id="main-product-image">
{% else %}
  <p>No image available</p>
{% endif %}

</div>
<div class="col-md-6">



</div>
</div>
</div>
</div>
